<resources>

    <!-- Base application theme. -->
    <style name="Theme.FragmentLifeCycle" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!-- Removed textcolor_dark_selector as it was not defined and might cause issues -->
        <!-- <item name="android:textColor">@color/textcolor_dark_selector</item> -->
    </style>

</resources>
